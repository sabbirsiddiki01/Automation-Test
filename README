
## Setup

1. **Clone the repository**:
    ```bash
    git clone https://github.com/your-username/saucedemo-tests.git
    cd saucedemo-tests
    ```

2. **Install dependencies**:
    ```bash
    npm install
    ```

3. **Configure WebdriverIO**:
    Ensure the `wdio.conf.js` file is properly configured for your environment.

## Running Tests

To run the tests, use the following command:
```bash
npm test

